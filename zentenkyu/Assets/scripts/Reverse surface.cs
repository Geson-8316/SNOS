using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Reversesurface : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        string text = "Hello";
            Debug.Log(text);
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
